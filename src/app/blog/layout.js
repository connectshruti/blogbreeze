export const metadata = {
  title: {
    default: "Blogs",
    template: "%s | Blogs",
  },
  description: "blogs blogbreeze",
};
const Layout = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
